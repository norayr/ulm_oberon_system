DEFINITION WebMRTestObjects;

   IMPORT
      WebObjects;
      
   TYPE
      Object = POINTER TO ObjectRec;
      ObjectRec =
         RECORD
            (WebObjects.ObjectRec)
         END;
   
      Handle = POINTER TO HandleRec;
      HandleRec =
         RECORD
            (WebObjects.HandleRec)
         END;
         
   PROCEDURE Create(VAR object: WebObjects.Object);
      
END WebMRTestObjects.
