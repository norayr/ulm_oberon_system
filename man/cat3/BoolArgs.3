


BoolArgs(3)            Ulm's Oberon System            BoolArgs(3)


NNAAMMEE
       BoolArgs - arguments of type BBOOOOLLEEAANN

SSYYNNOOPPSSIISS
       TYPE Value = POINTER TO ValueRec;
       TYPE ValueRec =
          RECORD
             (Args.ValueRec)
             bool: BOOLEAN;
          END;
       VAR type: Args.Type;

       TYPE ErrorEventRec =
          RECORD
             (Events.EventRec)
             name: ARRAY 80 OF CHAR;
          END;
       VAR error: Events.EventType;

       PROCEDURE Create(VAR value: Args.Value; bool: BOOLEAN);


DDEESSCCRRIIPPTTIIOONN
       _B_o_o_l_A_r_g_s implements the BBOOOOLLEEAANN argument type for _A_r_g_s_(_3_).
       _B_o_o_l_A_r_g_s accepts the set of alias names for TTRRUUEE and FFAALLSSEE
       of  _B_o_o_l_D_i_s_c_i_p_l_i_n_e_s_(_3_).   Note that this discipline may be
       specified for the argument list, the  individual  boolean-
       typed  argument, or the streams where it will be read from
       by the scanning modules.  The  help  text  for  this  type
       gives the set of known aliases.

       _C_r_e_a_t_e allows to create and set a BBOOOOLLEEAANN-typed value.

DDIIAAGGNNOOSSTTIICCSS
       An event of type _e_r_r_o_r is raised and related to the stream
       if the input could not be interpreted as TTRRUUEE or FFAALLSSEE.

SSEEEE AALLSSOO
       _A_r_g_s_(_3_)              general abstraction for arguments
       _B_o_o_l_D_i_s_c_i_p_l_i_n_e_s_(_3_)   sets of  alias  names  for  TTRRUUEE  and
                            FFAALLSSEE

BBUUGGSS
       There  might  be  some confusion if the set of alias names
       for BBOOOOLLEEAANN values has been set differently for the stream
       used  by  one of the scanners than for the stream used for
       printing the help text.










Release 0.5         Last change: 13 April 1995                  1


